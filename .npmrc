# better-sqlite3 13 ships an N-API binary but its lifecycle script still invokes node-gyp.
# Keep installs reproducible on machines without a compiler; `npm run setup` verifies the
# bundled SQLite binary and downloads Electron using Electron's own installer.
ignore-scripts=true
